/**
 * Layout du détail d'une position.
 * La metadata est générée dynamiquement dans `page.tsx` via `generateMetadata()`
 * — ce layout se contente de laisser passer les enfants.
 */
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
